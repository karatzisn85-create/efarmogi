# Εκκρεμότητες — Βελτίωση απόκρισης ERGOHUB

Τελευταία ενημέρωση: Μάιος 2026  
Έκδοση αναφοράς: 1.3.15

Σκοπός του εγγράφου: καταγραφή ευκαιριών βελτίωσης ταχύτητας/απόκρισης, χωρίς δέσμευση υλοποίησης.  
Σχετική αρχιτεκτονική: Electron main (`public/electron.js`) → IPC → React renderer (`src/`).

---

## Σύνοψη

Η εφαρμογή είναι κυρίως **I/O-bound** (σάρωση δίσκου, συγχρονική ανάγνωση JSON) και **IPC-amplified** (πολλά ξεχωριστά `invoke` από τον renderer). Τα μεγαλύτερα κέρδη προέρχονται από:

1. Λιγότερες σαρώσεις δίσκου στο main process  
2. Λιγότερα / έξυπνα IPC (ιδίως locks)  
3. Lazy loading UI και virtualization λιστών  

---

## Τρέχοντα σημεία συμφόρησης

### 1. Πλήρης σάρωση `dataDir` — `loadAllProjects` (ΥΨΗΛΟ)

**Αρχείο:** `public/electron.js` — `loadAllProjects`

**Τι γίνεται:** Για κάθε φάκελο έργου → κάθε υποέργο → `readFileSync` + `JSON.parse` στο `data.json`, συγχρόνως στο main thread. Επίσης `isProjectLocked` ανά έργο.

**Πού καλείται:**
- `load-all-projects` (Dashboard)
- `get-all-supervisors` (ξανά πλήρης σάρωση)
- `save-project-data` (matching τίτλων)

**Σύμπτωμα:** Αργό άνοιγμα / refresh όταν υπάρχουν πολλά έργα και υποέργα.

---

### 2. Περιττά IPC για locks (ΥΨΗΛΟ)

**Αρχεία:** `src/components/Dashboard.js`, `public/electron.js` (`check-project-lock`)

**Τι γίνεται:** Το `load-all-projects` **ήδη** επιστρέφει `isLocked` / `lockedBy` ανά έργο. Μετά το Dashboard καλεί ξανά `check-project-lock` **ανά γραμμή υποέργου** (όχι ανά `projectId`).

**Παράδειγμα:** 200 υποέργα σε 40 έργα → ~200 IPC αντί για ~40.

**Επαναλήψεις:**
- `loadDataWithCache` (batches 20)
- `loadProjects`
- Event `locks-changed`
- Polling / silent refresh

**Σύμπτωμα:** Καθυστέρηση μετά το load, «βαριά» αίσθηση στο UI ακόμα κι όταν τα δεδομένα έχουν ήδη φορτωθεί.

---

### 3. Eager φόρτωση όλων των domains στο startup (ΥΨΗΛΟ)

**Αρχείο:** `src/components/Dashboard.js` — `loadDataWithCache`

**Τι γίνεται:** Στο mount παράλληλα IPC για:
- έργα (`load-all-projects`)
- εντάξεις (`load-all-entaxeis`)
- προσκλήσεις (`load-all-proskliseis`)
- εγκρίσεις / links (`load-egkriseis-data`, `load-egkrisi-links`, `load-subproject-links`)
- + enrichment locks

**Σύμπτωμα:** Ο χρήστης περιμένει όλα ακόμα κι αν βλέπει μόνο τη λίστα έργων.

---

### 4. Monolithic Dashboard & bundle (ΥΨΗΛΟ — cold start)

**Αρχεία:** `src/components/Dashboard.js`, `craco.config.js`, `src/App.js`

**Τι γίνεται:**
- ~5.600 γραμμές σε ένα component
- Static imports: `EntaxisManager`, `ProsklisisManager`, `EgkriseisManager`, `TaskAssignmentManager`, `Statistics` (Chart.js), `PDFViewer`, κ.λπ.
- Το `App.js` κάνει `React.lazy` μόνο για το Dashboard· όχι για τα children → όλα στο ίδιο chunk

**Σύμπτωμα:** Αργό πρώτο άνοιγμα, μεγάλο `main.*.js`, αργό parse JS.

---

### 5. EgkriseisManager — ένα IPC ανά έργο (ΥΨΗΛΟ όταν ανοίγει module)

**Αρχεία:** `src/components/EgkriseisManager.js`, handler `load-project-egkriseis` στο `electron.js`

**Τι γίνεται:** `loadAllEgkriseis` κάνει **σειριακά** `invoke('load-project-egkriseis', projectId)` για κάθε μοναδικό έργο. Επιπλέον `check-entity-lock` ανά έγκριση.

**Σύμπτωμα:** Αργό άνοιγμα οθόνης εγκρίσεων με πολλά έργα.

---

### 6. Αναδρομική αναζήτηση αρχείων (ΥΨΗΛΟ — μεγάλοι φάκελοι)

**Αρχείο:** `public/electron.js` — `searchFiles` και παρόμοια

**Τι γίνεται:** Recursive `readdirSync` / `statSync` κάτω από `ΑΡΧΕΙΑ ΥΠΟΕΡΓΟΥ` όταν δεν βρίσκεται ακριβές path.

**Σύμπτωμα:** Κόλλημα main process σε άνοιγμα/λήψη αρχείου σε βαθιά δέντρα.

---

### 7. Λίστα υποέργων χωρίς virtualization (ΜΕΣΑΙΟ–ΥΨΗΛΟ)

**Αρχεία:** `src/components/Dashboard.js`, `ProjectCard.js`

**Τι γίνεται:** Render όλων των `ProjectCard` ταυτόχρονα. `<Statistics />` (Chart.js) πάντα mounted. Βαρύς υπολογισμός ανά κάρτα σε group headers (`entaxeis.find`, helpers).

**Σύμπτωμα:** Αργό scroll / αλλαγή φίλτρων με 100+ υποέργα.

---

### 8. Διπλά reload paths (ΜΕΣΑΙΟ)

**Αρχεία:** `Dashboard.js`, `AdvancedFilters.js`

**Τι γίνεται:**
- `loadDataWithCache` (με cache ~5 λεπτών) vs `loadProjects` (παρόμοια πλήρης φόρτωση)
- Μετά από mutations συχνά `loadDataWithCache(true)` / `loadProjects` αντί στοχευμένης ενημέρωσης
- `get-all-supervisors` ξανατρέχει `loadAllProjects` ενώ τα projects μπορεί να είναι ήδη στη μνήμη

---

## Τι βοηθά ήδη (να διατηρηθεί)

| Μηχανισμός | Πού |
|------------|-----|
| Cache 5 λεπτών στο `loadDataWithCache` | `Dashboard.js` |
| Debounced quick search (~300ms) | `Dashboard.js` |
| `useMemo` για `visibleProjects`, `groupedProjects` | `Dashboard.js` |
| Batching lock checks (20-άδες) | `Dashboard.js` |
| Async `fs.promises` σε `load-all-entaxeis` (μερικώς) | `electron.js` |
| `safeWriteJSON` (atomic write + backup) | `public/safeWrite.js` |
| Vendor splitting στο Craco | `craco.config.js` |

---

## Προτεινόμενες ενέργειες

### Γρήγορα κέρδη (χαμηλός κίνδυνος, 1–3 ημέρες)

| # | Ενέργεια | Impact | Αρχεία |
|---|----------|--------|--------|
| A1 | **Εμπιστοσύνη στο `isLocked` από `load-all-projects`** — αφαίρεση δεύτερου pass locks στο ίδιο refresh | Υψηλό | `Dashboard.js` |
| A2 | **Dedupe locks ανά `projectId`** — ένα status ανά έργο, όχι ανά υποέργο | Υψηλό | `Dashboard.js` |
| A3 | Νέο IPC **`get-all-project-locks`** (map `projectId → lock`) ή επιστροφή locks μόνο από `load-all-projects` | Υψηλό | `electron.js`, `preload.js`, `Dashboard.js` |
| A4 | **`React.lazy`** για managers/modals (Entaxis, Prosklisis, Egkriseis, Tasks, Statistics, PDF) | Υψηλό (startup) | `Dashboard.js` |
| A5 | **`React.memo(ProjectCard)`** + σταθερά callbacks (`useCallback`) για handlers | Μεσαίο | `ProjectCard.js`, `Dashboard.js` |
| A6 | **`get-all-supervisors`** από in-memory projects όταν υπάρχουν, όχι νέα `loadAllProjects` | Μεσαίο | `AdvancedFilters.js`, `Dashboard.js` |
| A7 | Μείωση polling locks — μόνο unique `projectId`, μεγαλύτερο interval αν δεν αλλάζει τίποτα | Μεσαίο | `Dashboard.js` |

---

### Μεσαίας προτεραιότητας (3–7 ημέρες)

| # | Ενέργεια | Impact | Σημειώσεις |
|---|----------|--------|------------|
| B1 | **Virtualized grid** (`react-window` / `react-virtuoso`) για λίστα υποέργων | Υψηλό (πολλά έργα) | Μεγάλη βελτίωση scroll |
| B2 | **Cache `loadAllProjects` στο main** — invalidation on save/delete (`mtime` ή version counter) | Υψηλό | Χρειάζεται προσεκτικό invalidation |
| B3 | **Lazy load** entaxeis / proskliseis / egkriseis links μόνο όταν ανοίγει το αντίστοιχο module/tab | Μεσαίο | Λιγότερα IPC στο startup |
| B4 | **Egkriseis:** ένα IPC `load-all-egkriseis-summary` αντί loop `load-project-egkriseis` | Υψηλό (module) | Νέος handler + migration |
| B5 | **Ενοποίηση reload API** — μία `refreshDashboard({ scope })` αντί `loadProjects` + `loadDataWithCache` | Μεσαίο | Λιγότερα bugs, καθαρότερο state |
| B6 | **Statistics** mount μόνο όταν ο χρήστης ανοίγει panel στατιστικών | Μεσαίο | Chart.js off critical path |

---

### Μακροπρόθεσμα (εβδομάδες+, αρχιτεκτονική)

| # | Ενέργεια | Impact | Ρίσκο |
|---|----------|--------|-------|
| C1 | **Index manifest** (`projects-index.json`) ενημερωμένο on save — αποφυγή full tree walk | Πολύ υψηλό | Μεσαίο — migration + sync |
| C2 | **Async FS** (`fs.promises`) / worker thread για σαρώσεις | Υψηλό | Μεσαίο — refactor handlers |
| C3 | **File index** ανά υποέργο (όχι recursive search σε runtime) | Υψηλό (αρχεία) | Μεσαίο |
| C4 | **Διαίρεση Dashboard** σε routes / feature folders με code splitting | Υψηλό (maintainability + perf) | Χαμηλό–μεσαίο |
| C5 | Προαιρετικά: embedded DB / SQLite για queries — **μόνο με ρητή απόφαση** (αντίθετο με τρέχον JSON model) | Πολύ υψηλό | Υψηλό |

---

## Προτεραιοποίηση (προτεινόμενη σειρά)

```
Φάση 1 (γρήγορα):  A1 → A2 → A3 → A4
Φάση 2 (λίστες):    B1 → A5 → B6
Φάση 3 (δίσκος):    B2 → B4 → B3
Φάση 4 (μακροπρ.): C1 → C2 → C3
```

---

## Τι να μην αλλάξουμε για «ταχύτητα»

- **`safeWriteJSON`** — atomic write + `.bak` rotation (ακεραιότητα δεδομένων)
- **Server-side authorization** στο `electron.js` — ποτέ έμπιστο role από renderer
- **Lock validation** (`isLockValid`, stale cleanup) — κίνδυνος ταυτόχρονης επεξεργασίας
- **Δομή φακέλων** UUID / ελληνικά ονόματα χωρίς migration plan
- **Audit log** σε destructive actions

---

## Μετρήσεις (πριν/μετά βελτιώσεις)

Προτείνεται logging (προσωρινό, dev only):

1. **Main:** χρόνος `loadAllProjects` (ms, αριθμός projects/subprojects)
2. **Renderer:** αριθμός `check-project-lock` invocations στο startup (στόχος: ~0 μετά A1–A3)
3. **React Profiler:** αλλαγή φίλτρου με 100+ `ProjectCard`
4. **Main block:** άνοιγμα αρχείου σε υποέργο με μεγάλο `ΑΡΧΕΙΑ ΥΠΟΕΡΓΟΥ`

---

## Σχετικά με άλλες εκκρεμότητες

Αυτό το αρχείο αφορά **απόδοση/απόκριση** μόνο. Για λειτουργικές εκκρεμότητες (features, bugs) προτείνεται ξεχωριστή ενότητα ή linked issues.

---

## Κατάσταση υλοποίησης

| ID | Περιγραφή | Κατάσταση |
|----|-----------|-----------|
| A1 | Εμπιστοσύνη `isLocked` από `load-all-projects` | ✅ Ολοκληρώθηκε |
| A2 | Dedupe locks ανά `projectId` | ✅ Ολοκληρώθηκε |
| A3 | Νέο IPC locks (παραλείφθηκε — καλύπτεται από A1+A2) | ⏭️ Δεν απαιτείται |
| A4 | `React.lazy` managers/modals | ✅ Ολοκληρώθηκε |
| A5 | `React.memo(ProjectCard)` | ✅ Ολοκληρώθηκε |
| A6 | `get-all-supervisors` από in-memory projects | ✅ Ολοκληρώθηκε |
| A7 | Μειωμένο polling locks (30s, unique projectId) | ✅ Ολοκληρώθηκε |
| B1–B6 | Μεσαίας προτεραιότητας | ⏳ Εκκρεμεί |
| C1–C5 | Μακροπρόθεσμα | ⏳ Εκκρεμεί |

_Ενημέρωσε τον πίνακα «Κατάσταση» όταν ολοκληρωθεί κάθε στοιχείο._
